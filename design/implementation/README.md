# Implementation captures

Phone-sized Flutter renders generated by
`apps/mobile/tool/visual_capture_test.dart`. They provide a reviewable bridge
between the Claude Design Terrella reference and the native implementation.

Regenerate them only when an intentional UI change has been reviewed.
